hash h
name k : message
name n : message
name m : message
channel c

system out(c,h(n,k)).

goal collision_absurd :

  forall (tau:timestamp)
  such that tau=tau: (* TODO euf crashes if constraint is empty *)
  output@tau = h(m,k) => exists () : false.

Proof.

  euf 0.

Qed.
