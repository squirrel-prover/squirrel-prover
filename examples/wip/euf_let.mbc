hash h
name k : message
name n : message
name m : message
channel c

system let s = h(n,k) in out(c,s).

goal collision_absurd :

  forall (tau:timestamp),
  output@tau <> h(m,k).

Proof.
  simpl.
  euf M0.
Qed.
