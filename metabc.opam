opam-version: "2.0.4"
name: "metabc"
description: "metabc"
version: "0.1"
build: [
  [make]
  [make "test"] {with-test}
  [make "doc"] {with-doc}
]
depends: [
  "fmt"
  "ocamlgraph"
  "alcotest"
  "menhir"
]
