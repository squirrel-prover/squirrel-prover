hash h
name k:message
channel c
name n:message
name m:message
system null.

(* Test direct case *)
equiv test : h(diff(m,n),k),h(diff(n,m),k) .
Proof.
prf 1.

yesif 1.
simpl.

prf 0.
Qed.


system [test]  in(c,x); out(c,h(diff(<x,m>,<x,n>),k)); out(c,h(diff(<x,n>,<x,m>),k)).

equiv [left,test] [right,test] test2.
Proof.

induction t.
expandall.

fa 1. fa 2. prf 2.

yesif 2.
split.
case H0. case H0.

expandall.
fa 1; fa 2. prf 2. yesif 2.

split.
case H0.
depends A, A1.
case H0.
depends A, A1.
Qed.
