channel c

abstract ok : message
name n : message
axiom len_ok : len(ok) = len(n)

system out(c,n XOR ok).

equiv test.
Proof.
  induction t.
  expandall.
  fa 1; fa 2.
  xor 2.
  yesif 2.
  apply len_ok.
Qed.
