(executable
  (name squirrel)
  (libraries squirrellib)
  (modules squirrel))

(test
  (name test)
  (libraries squirrellib)
  (modules test)
  (action (run %{test} --compact))
  (deps (source_tree tests)) ;; some tests rely on *.sp files in tests/
  (instrumentation (backend bisect_ppx)))
